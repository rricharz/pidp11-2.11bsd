MYBIN="${HOME}/bin"

all: tek thome tgraph

tek: tek.c
	cc -o tek tek.c

thome: thome.c
	cc -o thome thome.c

tgraph: tgraph.c
	cc -o tgraph tgraph.c
install:
	cp tek ${MYBIN}; cp thome ${MYBIN}; cp tgraph ${MYBIN}

